// src/config.ts

// Define the base URL for the backend API
export const API_BASE_URL = 'http://localhost:8080'; // Adjust this to match your backend server's URL

// You can add other configuration variables here if needed 